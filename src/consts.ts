const msRootFrame = 1000 / 60
const defaultStageWidth = 32
const defaultStageHeight = 32
const gravity = 1
const is3D = true
const cameraPadding = 20
const joystickPxRange = 60

export { msRootFrame, defaultStageWidth, defaultStageHeight, gravity, is3D, cameraPadding, joystickPxRange }
